#ifndef __REMOVE_DUPLICATES_H__
#define __REMOVE_DUPLICATES_H__

// Works with memory already reserved by Python.
void remove_duplicates(const unsigned int *vin,
                       unsigned int *vout,
                       const int size);

#endif
